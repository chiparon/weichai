# Workspace Adapters

Workspace and symbol providers for the workflow composition root. The current adapter exposes the C# target fixture through `ModuleSymbolPort`; a future IDE-backed provider can replace it without changing the workflow UI.
