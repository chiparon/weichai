# Mock Adapters

Demonstration-only implementations of search, adaptation, and backfill workflow
ports. Workspace discovery and symbol resolution are owned by
`@forexplore/workspace-adapters`, so this package does not define the module tree.
